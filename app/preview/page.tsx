"use client";

// /preview renders the same sales page as the homepage, minus the
// returning-member redirect — handy for previewing copy/offer changes.
import RefugeSales from "@/app/components/RefugeSales";

export default function PreviewPage() {
  return <RefugeSales />;
}
